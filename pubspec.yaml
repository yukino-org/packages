name: utilx_desktop
description: "🔧 Contains desktop specific utility tools used in Dart projects related to Yukino Org."
version: 0.0.0

publish_to: none

environment:
    sdk: ">=2.12.0 <3.0.0"

dependencies:
    puppeteer:
        git:
            url: git://github.com/xvrh/puppeteer-dart.git
    utilx:
        git:
            url: git://github.com/yukino-org/packages.git
            ref: dart_utilx

dev_dependencies:
    devx:
        git:
            url: git://github.com/yukino-org/packages.git
            ref: dart_devx
