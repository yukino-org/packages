name: tenka
description: "🌌 An easy to use Tenka wrapper."
version: 1.0.5

publish_to: none

environment:
    sdk: ">=2.15.0 <3.0.0"

dependencies:
    http: ^1.0.0
    path: ^1.8.3
    tenka_runtime:
        git:
            url: https://github.com/yukino-org/packages.git
            ref: dart_tenka_runtime
    utilx:
        git:
            url: https://github.com/yukino-org/packages.git
            ref: dart_utilx

dev_dependencies:
    devx:
        git:
            url: https://github.com/yukino-org/packages.git
            ref: dart_devx
